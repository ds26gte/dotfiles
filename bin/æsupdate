# last modified 2020-03-20
# Dorai Sitaram

files="linuxaliases"

for f in $files; do
  if test ! -f $f -a -f $f.æs; then
    echo decrypting $f.æs
    xxdecrypt $f.æs > $f
  elif test $f -nt $f.æs; then
    echo encrypting $f
    xxencrypt $f > $f.æs
  fi
done
