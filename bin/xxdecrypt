# last modified 2020-05-15
# created 2019-10-04
# Dorai Sitaram

if test ! "$INSCRUTABLE"; then
  echo INSCRUTABLE not set; exit
fi

read -p "Password: " -s crypticNonsense

export crypticNonsense

$INSCRUTABLE -d -pass env:crypticNonsense -in $1
