# last modified 2014-08-14

cd ~/src/neovim
topdir=$(pwd)

make clean
cmake -DCMAKE_INSTALL_PREFIX:PATH=/usr/local
make

cd runtime/doc
gcc doctags.c -o doctags
./doctags *.txt | LANG=C LC_ALL=C sort > tags

cd $topdir
cd bin
strip nvim

cd $topdir
sudo make install
