# Last modified 2018-07-26

# Neovim's clipboard feature uses xsel, if present, over xclip. But xsel
# doesn't work with Neovim. Rather than delete the xsel executable, use this
# script to translate the xsel call to an xclip call.

if test $# -eq 3
then
  case $3 in
    -b) xclip -quiet -i -selection clipboard;;
    -p) xclip -quiet -i -selection primary;;
  esac
else
  case $2 in
    -b) xclip -o -selection clipboard;;
    -p) xclip -o -selection primary;;
  esac
fi 
